const DEFAULT_BACKEND_URL = 'http://localhost:3030';

export const URL_USER = `${DEFAULT_BACKEND_URL}/user`;

export const URL_AUTH = `${DEFAULT_BACKEND_URL}/auth`;

export const URL_TRANSACTION = `${DEFAULT_BACKEND_URL}/transaction`;

export const URL_TRANSACTION_HISTORY = `${DEFAULT_BACKEND_URL}/transaction/history`;
